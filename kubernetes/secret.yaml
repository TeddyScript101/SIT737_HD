# Create this secret before deploying.
# Replace <base64-encoded-secret> with: echo -n 'your-strong-secret' | base64
#
# Or use kubectl directly (recommended):
#   kubectl create secret generic diary-secrets \
#     --from-literal=jwt-secret='your-strong-secret-here' \
#     -n diary-app
apiVersion: v1
kind: Secret
metadata:
  name: diary-secrets
  namespace: diary-app
type: Opaque
data:
  jwt-secret: OmpD4rY3NTJ5QWdzZWNyM1R0M2RpeQ==
